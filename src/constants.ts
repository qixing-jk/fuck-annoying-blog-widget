/**
 * 油猴脚本在浏览器中存储配置时使用的主键。
 * 建议在有重大数据结构变更时修改此值，以避免旧数据格式引发错误。
 */
export const STORAGE_KEY = 'site_configs'

/**
 * 注入到页面中，用于承载 React 设置面板的根元素的 ID。
 */
export const ROOT_ELEMENT_ID = 'fuck-annoying-blog-widget-script-root'

export const DEFAULT_NS = 'common'
